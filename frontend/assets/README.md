# Lionyx-E startup video asset

Place the branded startup video at:

`frontend/assets/lionyxe-startup.mp4`

Expected behavior:
- full-screen launch playback
- autoplay once on startup
- automatic transition into main app after completion

If the file is missing, the app falls back to a timed splash and then opens the main interface.
